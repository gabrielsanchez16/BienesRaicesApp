extends ./layout/app-admin 

block contenido 


    section(class="py-5")
        h2(class="text-center text-4xl font-extrabold pt-10")= pagina
        if propiedades.length
            div(class="grid grid-col-1 lg:grid-cols-3 md:grid-cols-2 gap-5 mt-14")
                each propiedad in propiedades 
                    include ./templates/card-admin
        else 
            p(class="text-center text-gray-800 text-sm py-10") No hay resultados